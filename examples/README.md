# Examples

Versioned runnable example configurations live in `../configs/`. The frozen
demo output in `../artifacts/demo/` contains a canonical aggregate manifest,
self-contained HTML Safety Card, runtime diagnostics, environment identity, and
the exact replay configuration. No row-level data are included.
